import s from './HomeView.module.css';

const HomeView = () => (
  <div className={s.wrapper}>
    <h1 className={s.heading}>Hi there! Please, sign in to continue</h1>
  </div>
);

export default HomeView;
